# Users Management Application

Веб-приложение для управления пользователями с REST API на Flask и фронтендом на Bootstrap.

## 🚀 Возможности

- ✅ REST API для управления пользователями (CRUD операции)
- ✅ Валидация данных с помощью Pydantic
- ✅ Современный веб-интерфейс на Bootstrap 5
- ✅ Контейнеризация с Docker и Docker Compose
- ✅ Покрытие тестами
- ✅ Логирование и обработка ошибок
- ✅ SQLite база данных
- ✅ Миграции

## 📋 Требования

- Python 3.12+
- Docker и Docker Compose (для запуска в контейнерах)
- pip (для локальной разработки)

## 🛠 Установка и запуск

### Вариант 1: Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Pobeda_project
```

2. Запустите приложение:
```bash
docker-compose up --build
```

3. Откройте в браузере:
   - **Frontend**: http://localhost:8080
   - **Backend API**: http://localhost:5000/api

### Вариант 2: Локальная разработка

#### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:
```bash
python run.py
```

Backend будет доступен на http://localhost:5000

#### Frontend

1. Откройте `frontend/index.html` в браузере
2. Или используйте простой HTTP-сервер:
```bash
cd frontend
python -m http.server 8080
```

Frontend будет доступен на http://localhost:8080

## 🧪 Тестирование

Запуск тестов:

```bash
cd backend
pytest
```

С покрытием кода:

```bash
pytest --cov=app --cov-report=html
```

Отчет о покрытии будет доступен в `htmlcov/index.html`
