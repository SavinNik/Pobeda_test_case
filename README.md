# Users Management Application

Полнофункциональное веб-приложение для управления пользователями с REST API на Flask и фронтендом на Bootstrap.

## 🚀 Возможности

- ✅ REST API для управления пользователями (CRUD операции)
- ✅ Валидация данных с помощью Pydantic
- ✅ Современный веб-интерфейс на Bootstrap 5
- ✅ Контейнеризация с Docker и Docker Compose
- ✅ Полное покрытие тестами
- ✅ Логирование и обработка ошибок
- ✅ SQLite база данных

## 📋 Требования

- Python 3.12+
- Docker и Docker Compose (для запуска в контейнерах)
- pip (для локальной разработки)

## 🛠 Установка и запуск

### Вариант 1: Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Pobeda_project
```

2. Запустите приложение:
```bash
docker-compose up --build
```

3. Откройте в браузере:
   - **Frontend**: http://localhost:8080
   - **Backend API**: http://localhost:5000/api

### Вариант 2: Локальная разработка

#### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите сервер:
```bash
python run.py
```

Backend будет доступен на http://localhost:5000

#### Frontend

1. Откройте `frontend/index.html` в браузере
2. Или используйте простой HTTP-сервер:
```bash
cd frontend
python -m http.server 8080
```

Frontend будет доступен на http://localhost:8080

## 📚 API Документация

### Endpoints

#### GET /api/users
Получить список всех пользователей

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com"
  }
]
```

#### GET /api/users/{id}
Получить пользователя по ID

**Ответ:**
```json
{
  "id": 1,
  "name": "John Doe",
  "email": "john@example.com"
}
```

#### POST /api/users
Создать нового пользователя

**Тело запроса:**
```json
{
  "name": "John Doe",
  "email": "john@example.com"
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "John Doe",
  "email": "john@example.com"
}
```

### Коды ответов

- `200 OK` - Успешный запрос
- `201 Created` - Пользователь успешно создан
- `400 Bad Request` - Неверные данные
- `404 Not Found` - Пользователь не найден
- `500 Internal Server Error` - Ошибка сервера

## 🧪 Тестирование

Запуск тестов:

```bash
cd backend
pytest
```

С покрытием кода:

```bash
pytest --cov=app --cov-report=html
```

Отчет о покрытии будет доступен в `htmlcov/index.html`

## 📁 Структура проекта

```
Pobeda_project/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   └── routes.py          # API endpoints
│   │   ├── config/
│   │   │   └── settings.py        # Конфигурация
│   │   ├── database/
│   │   │   └── setup.py           # Настройка БД
│   │   ├── models/
│   │   │   └── user_models.py     # Модели данных
│   │   ├── schemas/
│   │   │   └── user_schemas.py    # Pydantic схемы
│   │   └── __init__.py            # Фабрика приложения
│   ├── tests/
│   │   ├── conftest.py            # Фикстуры для тестов
│   │   └── test_routes.py         # Тесты API
│   ├── Dockerfile
│   ├── requirements.in
│   ├── requirements.txt
│   └── run.py                     # Точка входа
├── frontend/
│   ├── assets/
│   │   ├── css/
│   │   │   └── style.css
│   │   └── js/
│   │       └── main.js
│   ├── Dockerfile
│   ├── nginx.conf
│   └── index.html
├── docker-compose.yml
├── .gitignore
└── README.md
```

## 🔧 Конфигурация

### Переменные окружения

Backend поддерживает следующие переменные окружения:

- `DATABASE_URL` - URL базы данных (по умолчанию: `sqlite:///instance/database.db`)
- `SECRET_KEY` - Секретный ключ Flask (по умолчанию: `dev`)
- `LOG_LEVEL` - Уровень логирования (по умолчанию: `INFO`)
- `FLASK_ENV` - Окружение Flask (`development` или `production`)

## 🐛 Отладка

### Логи

Логи приложения выводятся в stdout. В Docker:

```bash
docker-compose logs -f backend
```

### База данных

SQLite база данных хранится в `backend/instance/database.db`

Для просмотра:
```bash
sqlite3 backend/instance/database.db
```

## 🚢 Production Deployment

Для продакшена рекомендуется:

1. Использовать Gunicorn вместо Flask dev server:
```dockerfile
CMD ["gunicorn", "run:app", "-b", "0.0.0.0:5000", "-w", "4"]
```

2. Использовать PostgreSQL вместо SQLite:
```bash
DATABASE_URL=postgresql://user:password@localhost/dbname
```

3. Настроить HTTPS и reverse proxy (nginx)

4. Установить переменные окружения:
```bash
SECRET_KEY=<strong-random-key>
FLASK_ENV=production
LOG_LEVEL=WARNING
```

## 📝 Лицензия

MIT

## 👥 Автор

Тестовое задание для Pobeda
